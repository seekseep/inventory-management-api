{
  "name": "inventory-management-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply inventory-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply inventory-db --remote",
    "seed": "tsx scripts/seed.ts",
    "api-keys": "tsx scripts/api-keys.ts"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.6.1",
    "commander": "^14.0.3",
    "drizzle-orm": "^0.39.0",
    "hono": "^4.7.0",
    "inquirer": "^13.4.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "@types/inquirer": "^9.0.9",
    "drizzle-kit": "^0.30.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.10.0"
  }
}
